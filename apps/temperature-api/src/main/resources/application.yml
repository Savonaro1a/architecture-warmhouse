server:
  port: 8081

spring:
  application:
    name: temperature-api

logging:
  level:
    com.warmhouse.temperatureapi: DEBUG
    org.springframework.web: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
